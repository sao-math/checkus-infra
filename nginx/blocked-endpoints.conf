# 2026-05-04 incident mitigation removed.
# Root cause (task_template missing covering index) fixed by index addition + idx_is_leaf drop.
# Service stable since 13:09 UTC (0 leaks, healthy pool). TV endpoints re-enabled.
